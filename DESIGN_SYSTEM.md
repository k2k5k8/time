# DESIGN_SYSTEM

## 视觉方向

- 安静、可信、轻量；数字是卡片第一视觉层级。
- 未来事件使用明亮主题色，过去事件使用沉稳主题色；不能只依赖颜色区分状态。
- 顶部保留产品名、分类入口、搜索和设置；底部使用轻量触控栏承载后续功能入口，不做多标签底部导航。
- 顶部栏在普通状态使用 44dp 高的暖白悬浮圆角容器；背景不透明度需足以遮住 Hero 中的强水平细节，通过柔和阴影和极浅边界与主体区域分离；不使用大面积纯色横幅。
- 首页主体使用淡灰色 `#F3F5F7`，卡片和底部触控栏使用白色形成层次。
- 首页不显示分类筛选条，分类筛选统一放在左侧抽屉中。

## 首页背景融合层（2026-08-21）

- 首页背景不是“图片 + 纯白内容区”，而是 `背景图片 → 环境雾化层 → 动态 UI 基底 → 纸张卡片` 的连续空间。
- `AdaptiveBackgroundPaletteAnalyzer` 对 Hero 图片做低分辨率采样，提取环境色、明暗度、饱和度和复杂度；分析结果只在图片加载时计算并缓存，不逐帧分析。
- `AdaptiveTransitionLayer` 使用背景环境色、低频模糊和 Quiet Zone 生成渐变；卡片区域不得回退到纯白背景。
- 卡片外层使用半透明暖灰/米白纸张材质、环境色反射和低对比环境阴影；现有卡片模板只作为内容层保留。
- Hero 与网格仍共用单一 `LazyVerticalGrid` 滚动容器；卡片顶部向 Hero 空间侵入约 36–52dp，Small/Wide 两列与跨列规则不变。
- 顶层模板阴影不再使用黑色 Material elevation，由 `AdaptiveCardSurface` 提供背景色相关的柔和阴影。

### AdaptiveBackgroundPalette 参数

- 背景图采样：最大约 `32×32` 有效采样点，底部区域权重略高于顶部。
- 过渡层：环境色透明度约 `8%–28%`，下部动态 UI 基底透明度约 `82%–100%`。
- 卡片外层：表面透明度约 `86%`，背景透射/环境色反射约 `7%–14%`，顶部暖白高光约 `14%`。
- 卡片顶部圆角 `28dp`，外层边界 `1dp`，内层保留各模板自身圆角与排版。
- 环境阴影使用提取环境色混合中性深灰，外层阴影高度约 `9dp`，不再使用固定黑色阴影。
- 背景切换淡入约 `850ms`；卡片出现使用约 `480ms` 的低位移淡入，避免弹出式 Dashboard 感。

## 阶段 1 token 与阶段 2 语义实现

- 主色：Material 3 `primary`
- 页面背景：Material 3 `surface`
- 卡片圆角：18dp
- 卡片阴影：2dp，保持轻量层次
- 页面水平边距：20dp
- 卡片间距：12dp
- 筛选条间距：8dp
- 主数字：DisplaySmall / 半粗
- 标题：TitleMedium
- 辅助信息：BodySmall
- 页面背景、容器表面、边框和文字现在使用 Material 3 `colorScheme` 语义 token；卡片的蓝/橙标题色仍保留为视觉角色，卡片主体/底栏跟随当前主题表面色。
- 主题模式为 `SYSTEM / LIGHT / DARK`；阶段 3 已提供 DataStore，阶段 4 已接入设置页恢复与写回，阶段 5/6/7 的事件操作不改变主题 token。

## 卡片网格

- 主页模板网格固定为两列：Small 占一个槽位，因此每排两张；Wide 跨满两个槽位。Small 采用更高的纵向比例和独立紧凑排版，禁止仅缩小 Wide 的字号；网格水平和垂直间距统一为 12dp。
- 长按任一卡片约 500ms 进入沉浸式“编辑布局”。被拿起的卡片上浮约 6dp、缩放至约 `1.055`、按横向拖动最多倾斜 `±3°`；其余卡片不缩小，仅以弱倾斜与弹性位移表达可避让状态。不得显示蓝色网格线、工程化选框或密集工具栏。
- 卡片接近有效位置时用极浅的纸张高光表示占位；松手只结束拖拽，不退出编辑模式，“完成”或返回才保存并退出。布局以 `cardId / order / gridWidth / gridHeight` 保存到本地 DataStore，现有 Small/Wide 分别映射为 1 / 2 列。
- 时间主体使用更大的 `Lantinghei TC Heavy / 兰亭黑-繁 特黑`，默认 38sp；窄列根据可用宽度自动缩小。
- 当前字体来自本机 macOS 系统字体资源，仅用于本地原型验证；正式发布前需确认字体再分发授权。

## 三段式事件卡片

每个事件使用一个圆角矩形，并按垂直方向拆成三个区域：

1. 标题栏：未来和顶部标题使用 `#5ED1FF`，过去使用 `#FFBC00`；因底色较亮，标题与图标使用深色；标题右侧显示“还有”或“已经”。
2. 时间主体：白底黑字，只显示主数字和单位，承担第一视觉层级。
3. 日期底栏：浅灰底深灰字，显示起始/终止日期和全天语义。

当前通过 `cardPaletteKey` 选择整套卡片颜色，暂不开放颜色编辑；未来可将标题、主体文字、主体背景、日期背景和日期文字分别变成用户配置。

## 统一模板内容契约

- 每个模板必须通过 `rememberTimeCardPresentation(event)` 读取统一展示数据：大标题、小标题、实时倒计时、起始时间与分组。
- 小标题和分组允许由某种视觉样式隐藏，但不允许新增模板私有的标题、日期或分组字段；事件设置页中的同一组数据必须能切换到所有模板。
- 倒计时必须使用共享 `EventTimeCalculator` 的实时结果，不得使用样例中的静态 `relativeLabel` 或写死数字。

## 旅行纪念日模板

- 新增模板键：`TRAVEL_MINIMAL_EDITORIAL`，默认用于首页的“东京旅行”事件；`TRAVEL_SUNSET_GLASS` 和手账模板继续保留。
- 新模板采用奶油白哑光材质、暖棕文字、极简飞机虚线轨迹、两条 editorial 分割线和大留白；不使用城市照片、强玻璃透明、高饱和色或复杂装饰。

- `TRAVEL_SUNSET_GLASS` 继续保留为上一版黄昏玻璃风模板，并在网格中跨列展示，保证已有事件外观不被破坏。
- 卡片使用本地东京黄昏柔焦摄影背景：左侧暖白天空保留文字呼吸区，右侧展示富士山、东京塔和虚化城市；上方叠加暖白半透明渐变与极细白色描边，保持玻璃拟态但不产生硬反光。
- `WIDE` 与 `SMALL` 是两套布局：宽卡片展示完整左对齐信息层级；小卡片压缩内边距和字号，但仍保留标签、分组、标题、数字和日期。
- 视觉层级为标签胶囊、分组信息、主标题、超大渐变数字、日期行；数字使用偏轻字重，避免粗黑体抢夺摄影氛围。
- 倒计时通过统一 `EventTimeCalculator` 根据 `targetDate` 计算，页面由共享的 `rememberEventCountdown` 状态每分钟刷新一次；不再把 `28` 作为运行时倒计时数据。
- `TravelCardConfig` 已预留标题、标签、分组、背景预设、目标日期、单位、地点标签、胶囊图标、日期图标和尺寸字段。

### 可配置字段

`TravelCardConfig` 是模板的唯一内容入口：`title`、`badgeLabel`、`groupLabel`、`targetDate`、`countdownUnit`、`badgeIcon`、`dateIcon` 和 `size` 均不写死在 UI 中。事件设置页已提供标题、标签、分组、日期、顶部 icon 和 Small/Wide 的实时预览编辑；模板画廊预览刻意不显示分组，避免将模板名称重复写入卡片。事件分组由 Room 事件事实字段提供，空分组和用户创建的分组名由 DataStore 保存。

旧旅行卡片保留各自的居中或拼贴排版；`TRAVEL_MINIMAL_EDITORIAL` 使用严格左对齐，时间主体使用 `HeadlineMedium + ExtraBold`，标题和日期之间保持宽松留白。卡片不再显示三点菜单，点击整张卡片进入事件设置页。

首页悬浮标题栏普通状态内容高度固定为 44dp，搜索展开状态为 64dp，其中 Material 3 输入框保留原生 56dp 高度以避免裁切；外侧保留 16dp 水平留白和 8dp 垂直留白。字体变化只能影响文字绘制，不得改变卡片第一部分的尺寸；首页网格与标题栏之间保留 16dp 余量。

当前经典卡片颜色：蓝色 `#5ED1FF`，橙色 `#FFBC00`；两种颜色统一使用深色文字保证可读性。首页悬浮标题栏使用暖白半透明材质，不继承卡片主题色。

## 模板浏览与搜索

- 模板浏览不是列表：舞台只绘制当前与相邻两张。中心卡 `scale=1 / alpha=1 / rotationX=0`；上方预览约 `-24°`、下方预览约 `+24°`，两者约 `0.87` 缩放、弱阴影与约 `42%` 透明度，并在舞台上下边缘隐去。透视必须直接作用于真实预览卡表面，而非其舞台容器，确保边缘可见梯形收拢。浏览区不再显示分类名称或介绍，只保留小型“模板 x / N”序号，把垂直空间留给卡片移动。
- 纵向相邻行与中心行必须共享同一 Small/Wide 槽位坐标，不能在“是否当前行”切换时替换横向索引。相邻行的左右尺寸预览透明度随其到中心的距离从 `0 → 1` 连续变化；因此向上、向下切换都先移动主卡，再自然展开左右预览，不允许左侧卡跳变。
- 上下拖动必须实时驱动位置、缩放、透明度和透视角，松手以约 `420ms`、`cubic-bezier(0.22, 1, 0.36, 1)` 停靠；首尾有界，不循环、不闪回。随机选择沿相同连续轨道移动到中心，不得直接替换卡片。
- 模板页顶部不用普通工具栏或独立色块：移除标题和滑动提示文字，只保留更贴近状态栏的左侧返回、右侧搜索和收藏三枚 56dp 暖白半透明圆形悬浮按钮，使用极浅白色描边和柔和阴影，并直接透出页面渐变；搜索激活时只切换为轻暖强调态，并在按钮下展开输入面。
- 首页与模板页的搜索展开为圆角半透明输入面：保留返回/关闭、自动焦点、清空、结果和空结果状态；首页的 Material 3 输入框不得低于 56dp，搜索图标使用低饱和强调色，输入边框保持透明，避免默认 Material 外框破坏悬浮栏层级。

## 底部时间轴导航

- 使用动态环境色半透明悬浮容器，圆角、轻描边和弱阴影与 Hero 背景融合，不使用传统实色 Tab Bar。
- 容器内部绘制横向时间轴主线，左侧“大事件”为默认高亮里程碑节点，右侧“日子簿”为较弱的日常记录节点；两页末端均增加同材质的 42dp 圆形加号。主页创建默认日期事件，日子簿创建预填当前选中日期。
- 首页与日子簿必须共用同一个轻量胶囊导航组件；“大事件”回到主页，“日子簿”进入月历页面，不再维护两套导航视觉。
- 选中态使用节点光晕、选中侧分段线条、移动光点和 220ms 内的颜色/尺寸过渡；轨道保持低对比度，避免横线成为永久视觉噪声。
- Hero 背景层延伸到悬浮导航后方，导航容器避让系统手势区，节点文字必须完整显示。
- 底部导航以覆盖层叠在全高列表上，不占用列表可视高度；导航背景限制在自身长扁圆角矩形内，卡片可在胶囊周围连续显示，列表底部安全滚动空间保证最后一张卡可完整停在胶囊上方。
- 普通浏览状态下顶部导航和底部时间轴导航均使用独立的长扁圆角背景面；背景只存在于各自胶囊范围内，搜索展开时顶部输入容器提高不透明度。
- 两个节点都必须保持可发现的中文无障碍语义。
- 两页加号均使用“新建事件”中文语义，并沿用同一新建逻辑；不再保留独立悬浮 FAB。

## 日子簿页面雏形（2026-08-21）

- 页面沿用首页/新增日期页的暖白、暖棕、奶油卡片和 Noto Serif SC 气质；日历容器使用 28dp 圆角、1dp 暖色描边和充足留白，避免传统密集表格感。
- 月份标题集中在顶部，左右切月按钮弱化为圆形图标；今天使用浅暖色圆形底，选中日期使用实色暖橙圆形底。
- 日期格下方的短横杠是日子簿主要视觉标记：系统节日为浅珊瑚色，用户记录为柔和蓝色，单日最多 3 条，超出显示 `+`。
- 详情区只呈现选中日期、星期和轻量事件行；没有内容时，整块空状态可点击新增。底部不再放置重复的“记下一件小事”胶囊，减少首屏冗余。
- 日子簿顶部不额外显示返回按钮和“日子簿”标题，只保留月份切换，优先将首屏空间给日历与当日内容；只有事件较多时才允许继续下滑。
- 日子簿外层背景容器必须应用 `statusBarsPadding()`，而不是仅给月份卡片增加固定 top margin，以适配刘海、挖孔和不同状态栏高度。
- 选中日已有事件时，在详情日期标题右侧显示 48dp 的轻量圆形加号；选中日为空时不重复显示此按钮，整块空状态承担新增动作。
- 日子簿的数据模型和页面状态与首页事件卡解耦，但用户事件可由 `TimeEvent.localDate/targetInstant` 映射；`showInMilestone` 不应默认开启，保证“日子簿完整记录 / 大事件精选节点”的产品分工。

## 状态

- 未来：文案以“还有”开头，并使用未来主题色。
- 过去：文案以“已经”开头，并使用沉稳色。
- 空状态：提供“记录一个已经开始的事件”和“期待一个未来事件”两个方向。
- 加载、错误和真正的持久化状态留到后续阶段。
- 分类抽屉提供状态/分组组合筛选和分组管理；底部导航只聚焦“大事件 / 日子簿”两个时间入口。

## 阶段 2 设计约束（已完成）

- 上述十六进制色值是浅色原型的视觉基准，不得直接作为所有主题的最终实现；阶段 2 为页面背景、悬浮栏、卡片主体、底栏、描边和文字建立语义化 Light/Dark token。
- 主题模式定义为 `SYSTEM / LIGHT / DARK`。阶段 2 保证状态与视觉正确；阶段 3 建立 DataStore，阶段 4 已完成设置页接入。
- 纯规划信息使用不可点击 ListItem/说明文本，不附空 `onClick`；可点击元素必须有 pressed/disabled 状态和真实结果。
- 搜索展开后必须有输入焦点、清空、关闭、有结果、无结果五种状态；返回键先关闭搜索，再离开页面。
- 阶段 2 已实现以上搜索状态；阶段 4 的搜索输入仍作用于当前 Room 读取结果，持久化搜索索引留给后续阶段。
- 在字体缩放 200% 时允许卡片增高、标题换行或重排，不能用裁剪主要信息来维持固定截图比例。
- 卡片对 TalkBack 应合并为完整语义单元；内部装饰 icon 不重复朗读，模板视觉不能破坏阅读顺序。
- 动画保持 150–250ms 的克制过渡，并尊重系统动画关闭/缩放设置；没有动画时仍必须有明确状态变化。
- 图片模板必须定义加载失败/资源缺失时的纯色或渐变 fallback，并保持文字对比度。

## 阶段 3 设计约束（已完成）

- Room 只保存事件事实字段和用户配置字段；相对时间、状态、星期等派生显示不得冗余落库。
- `ALL_DAY` 继续以 `LocalDate` 表示，`TIMED` 继续以 `Instant + ZoneId` 表示；Repository 不得改变这一语义。
- DataStore 的主题值只影响设置数据层；在正式 UI 接入前，不能让持久化层改变现有内存样例和 Small/Wide 两列网格行为。
- 迁移、软删除和未知设置值必须有可重复的 Android instrumentation test；没有迁移测试不得升级 schema。

## 阶段 4 设计约束（已完成）

- 首页真实事件来自 Room Flow；模板画廊是展示预览，不能因为点击预览而写入数据库。
- 主题设置恢复失败时保留当前 Compose 状态，不阻塞首页渲染。
- 事件设置编辑通过 Repository 写回，卡片显示继续由 `EventTimeCalculator` 派生。
- 接入持久化不得改变四类卡片、Small/Wide 两列网格和现有搜索/抽屉视觉层级。

## 阶段 5 设计约束（已完成）

- 置顶、归档和删除动作必须在事件设置页表达清楚，不能用无结果的占位按钮。
- 删除需要二次确认；归档/软删除后的活动首页应即时移除对应卡片。
- 在完整新增表单、校验和撤销路径准备完毕前，不新增可执行的创建入口。

## 阶段 6 设计约束（已完成）

- 底部导航的新建入口必须连接真实表单；标题为空或日期时间格式错误时，保存按钮不可用或明确显示错误。
- 全天和精确时刻使用不同输入语义，精确时刻保留设备 ZoneId，不在 UI 用毫秒除以一天计算。
- 保存后返回首页并保持现有卡片、搜索、抽屉和 Small/Wide 网格布局。
- 新增日期页默认使用全天；返回圆钮固定在滚动层上方。日期/时间在常规宽度并列等宽，在窄屏纵向排列；选择格文本只显示一行并使用省略号，不能因宽度不足挤压换行。

## 阶段 7 设计约束

- 删除后的 Snackbar 必须有明确的“撤销”动作，并避开首页底部导航加号的触控区域。
- 撤销只恢复最近一次软删除；不通过重新播种样例或改变事件事实字段实现。
- Snackbar 消失后不应留下不可见的悬空操作状态。

## 阶段 7B 质量加固约束

- 事件卡必须以“标题 + 时间状态 + 日期”的完整中文语义暴露给 TalkBack，并将点击动作绑定在同一个语义节点上。
- 搜索、设置、新建、分类与时间轴导航节点必须有明确的中文 content description；纯装饰图像保持无障碍隐藏。
- 关键首页入口和卡片在 130% 与 200% 字体缩放下不得从语义树消失；宽屏尺寸仍保持 Small/Wide 网格规则。
- 语义加固不得新增回收站、Widget、通知、网络或账号入口；规划项继续保持不可交互说明。
