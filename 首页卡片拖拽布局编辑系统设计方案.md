# 首页卡片拖拽布局编辑系统设计方案（重设计）

> 目标体验：iOS 主屏 Widget 编辑的「拿起—避让—吸附」物理感 + Notion Dashboard 的网格秩序。
> 落地载体：Moment Mark / 刻间，Jetpack Compose 首页（现有 `HomeFeature` + `CardBoard`）。
> 本文档是交互与架构规范，实施分阶段进行，不要求一次性替换现有 `reorderedCardLayouts`。

---

## 0. 现状与差距

现有实现（`app/src/main/java/com/cch/momentmark/ui/home/CardBoard.kt`）：

- `HomeCardLayout(cardId, order, gridWidth, gridHeight)` 已持久化到 DataStore，`gridHeight` 已作为 2×2 兼容接口预留 —— 数据层基础是对的，保留。
- `reorderedCardLayouts()` 只在松手时做一次列表 move，拖动过程中无占位符、无实时让位、无编辑态视觉。
- 缺失：编辑模式状态机、背景点阵、浮起/阴影反馈、实时重排、吸附动画、边缘自动滚动、undo/redo。

重设计原则：**保留 order 驱动的数据模型，替换交互层**。位置（row/column）由确定性打包算法从 order 派生，不直接存储，避免持久化漂移（详见第八节）。

---

## 一、完整交互流程

### 1.1 全流程时序

```
浏览态 (Idle)
   │ 用户点击「编辑首页」（或长按任意卡片 → 弹出菜单选「编辑布局」）
   ▼
编辑态 (EditIdle)  200~300ms 进入动画（见 6.1）
   │  背景点阵渐显 · 页面 scale 0.98→1 · 卡片轻微浮动 · 边缘出现编辑描边
   │  顶部出现「完成」按钮，卡片点击/打开事件全部禁用
   ▼
长按卡片 ≥ 150ms ────────────► 触觉反馈 (HapticFeedback.LongPress)
   ▼
浮起 (Lifted)  150ms：scale 1→1.05，阴影 elevation 升高，卡片「脱离纸面」
   │  原位置生成占位符（同尺寸、半透明、虚线边框）
   ▼
拖拽中 (Dragging)
   │  拖拽卡片绝对定位跟随手指（graphicsLayer.translationX/Y，不参与重组布局）
   │  占位符实时移动到「预测落点」
   │  其他卡片随预测落点做 spring 让位（连续拖动即连续重排，不等松手）
   │  靠近顶部/底部 < 80px 触发自动滚动
   ▼
松手 (Drop)
   │  velocity 低 → 就近吸附（< 卡片宽 30% 阈值）
   │  velocity 高 → 用速度外推预测落点，直接 snap
   ▼
落位动画 (Settling)  spring 收敛 + 占位符消隐
   ▼
回到编辑态 (EditIdle) ── 可继续拖下一张 ──┐
   │                                    │
   │ 点击「完成」                         │ 循环
   ▼                                    │
退出编辑 (220ms 反向动画) ── commit LayoutState → DataStore
   │
   ▼
浏览态 (Idle)，Snackbar 可选提示「布局已保存 · 撤销」
```

### 1.2 关键交互决策

| 决策点 | 方案 | 理由 |
|---|---|---|
| 进入编辑方式 | 按钮 + 长按快捷入口双通道 | 长按直达是 iOS 肌肉记忆；按钮保证可发现性 |
| 编辑态中卡片点击 | 全部禁用，仅长按可拖 | 防止误触打开事件详情打断编辑 |
| 占位符跟随策略 | 占位符 = 预测落点，实时移动 | 用户永远知道「松手后卡片会去哪」 |
| 重排时机 | 拖动中实时重排，不等松手 | 对应需求第五节「连续拖动排序」 |
| 撤销粒度 | 每次 Drop = 1 个 undo 单元 | 粒度太细（每次交换）会让 undo 失控 |

---

## 二、状态机设计

### 2.1 页面级状态机（EditSession）

```
         点击「编辑首页」            长按卡片(150ms+haptic)      位移>8dp(消抖)
  Idle ──────────────────► EditIdle ──────────────────► Lifted ────────────► Dragging
   ▲                          │  ▲                                                 │
   │      点击「完成」          │  │        落位动画收敛(spring静止)                     │
   └──────────────────────────┘  └───────────────── Settling ◄────────────────────┘
                                                      ▲  │
                                        速度外推/snap ──┘  └─ 占位符消隐 → EditIdle
```

| 状态 | 进入条件 | 页面表现 | 允许的转换 |
|---|---|---|---|
| `Idle` | 默认 | 正常浏览、滚动、点击 | → `EditIdle` |
| `EditIdle` | 点击编辑入口 | 点阵背景、卡片浮动呼吸、编辑描边、「完成」按钮 | → `Lifted`、→ `Idle`(退出) |
| `Lifted` | 长按卡片 | 单卡 scale 1.05 + 阴影 + haptic，占位符生成 | → `Dragging`、→ `EditIdle`(未移动即松手=取消) |
| `Dragging` | 位移超 8dp 消抖阈值 | 拖拽卡绝对定位，实时重排，边缘自动滚动 | → `Settling` |
| `Settling` | 松手 | snap/spring 落位，占位符消隐，commit 预备 | → `EditIdle` |

实现要点：
- 状态机是**纯 Kotlin 状态机**（`sealed interface EditSessionState`），放在非 Compose 的 domain 层，可单测。
- Compose 层只订阅状态渲染，手势层把原始指针事件翻译成状态机输入（`onPressStart / onMove / onRelease`），与 UI 解耦。

### 2.2 卡片级状态（渲染子状态）

| 子状态 | 变换 | 阴影 | z 序 |
|---|---|---|---|
| `Normal` | 无 | 无 | 0 |
| `Floating`（编辑态中） | y 微幅 ±2dp 呼吸（可选，低成本才做） | 低 | 0 |
| `Lifted` | scale 1.05 | 高（elevation 12dp 等效） | 1 |
| `Dragging` | scale 1.05 + translation 跟手 | 最高 + 轻微投影偏移 | 2（独立 overlay 层） |
| `Ghost`（占位符） | scale 1.0，alpha 0.35，虚线描边 | 无 | -1 |
| `Displacing`（让位中） | translation 由 spring 驱动到目标槽位 | 低 | 0 |

关键规则：**同一时刻最多一张卡片处于 `Dragging`**（多指触控见 7.6）；`Ghost` 只有一个；`Displacing` 动画可取消但不可叠加（见 7.7）。

---

## 三、拖拽算法

### 3.1 抓取（Grab）

```
onLongPress(card, pointerPosition):
    grabSession = DragSession(
        cardId        = card.id,
        originIndex   = card.currentOrder,
        originRect    = card.boundsOnScreen(),      // 用于松手取消时回弹
        grabOffset    = pointerPosition - card.center, // 手指相对卡片中心的偏移
        pointerId     = event.pointerId,             // 绑定手指，多指忽略
        startTime     = now()
    )
    // 卡片渲染切换到 overlay 层：layout 参数冻结为 originRect 尺寸，
    // 之后只更新 graphicsLayer.translationX/Y —— 不再触发 measure/layout
```

### 3.2 跟手（Track）

```
onMove(position, velocity):
    // 1. 位置更新：线性、无插值、每帧一次（对应 drag: linear）
    target = position - grabOffset
    target.x = clamp(target.x, minX, maxX)   // 布局区域边界（见 7.2）
    target.y = clamp(target.y, minY, maxY)
    dragCard.translation = target            // 只写 translation，走 RenderThread 图形层

    // 2. 速度采样：指数滑动平均，保留最近 100ms
    velocityTracker.add(position, now())

    // 3. 触发预测重排（节流，见 7.5）
    maybeReorder(dragCard.center)
```

**为什么 translationX/Y 而不是改 layout**：Compose 中修改 layout 参数会触发 measure + 递归 placement，60fps 下 50 张卡必卡；`Modifier.graphicsLayer { translationX = ... }` 的 lambda 版本把状态读取推迟到 placement 阶段之后，不触发重组、不触发重测量，直接走 GPU 合成。

### 3.3 释放（Drop）

```
onRelease():
    v = velocityTracker.estimate()
    if |v| > flingThreshold:                 // 高速甩动
        predictedCenter = dragCenter + v * 120ms   // 速度外推
    else:
        predictedCenter = dragCenter
    slot = nearestValidSlot(predictedCenter)       // 见第五节吸附
    animateDragCardTo(slot.rect, spec = snapSpec)  // ease-out snap
    commit(temporaryLayout)                        // 见第九节数据层
    dismissPlaceholder()
```

---

## 四、碰撞检测算法

### 4.1 判定规则（含滞回）

拖动卡片中心进入目标区域 **50% 以上**触发交换——等价于「拖拽中心点落入目标矩形按面积权重收缩后的核心区」。为防抖动，叠加三层保护：

```
core(target) = 以 target.center 为中心、边长 = target.size × 0.5 的矩形

shouldSwap(dragCenter):
    hit = 空间索引查询 dragCenter 所在 cell 的近邻卡片
    t   = 近邻中与 dragCenter 距离最近、且 dragCenter ∈ core(t) 的卡片
    if t == null:                       return false
    if t == lastSwapTarget:             return false   // 滞回：同一目标不重复触发
    if now() - lastSwapTime < 50ms:     return false   // 时间消抖：20fps 上限
    if dragCenter ∉ core(t) 持续帧数 < 2: return false  // 空间消抖：需连续 2 帧命中
    return true
```

「50% 重叠」用核心矩形近似，避免每帧做精确多边形相交——拖拽中心是点，点在 0.5 边长核心区内 ≈ 两矩形重叠过半，误差可忽略且更稳定。

### 4.2 空间索引（Spatial Hash）

50+ 卡片不遍历：网格坐标 → 卡片的哈希表，查询 O(1)。

```
class SpatialIndex(cellSize: Float) {     // cellSize = 卡片最小边长
    private val buckets = HashMap<Long, MutableList<Rect>>()

    fun insert(rect: Rect) {              // 一个卡片跨多个 cell 时全部登记
        for (cx in rect.cellsX()) for (cy in rect.cellsY())
            bucket(cx, cy).add(rect)
    }

    fun query(point: PointF): List<Rect> = bucket(point).toList()
}
```

移动端两列网格实际卡片同屏 ≤ 10 张，O(n) 遍历也够；SpatialIndex 是为 50+ 卡和多行跨度卡预留的，构建成本 O(n)，只在重排落位后重建一次（拖动中不重建，索引只服务于「静止卡片」）。

QuadTree 在移动端没必要（卡片都轴对齐、尺寸规整），Spatial Hash 更简单且无树平衡开销。

---

## 五、布局重排算法

### 5.1 核心机制：占位符 + 插入式预测重排

不做「两卡对调」（对调在变跨度网格中会产生非法布局），做**插入**：

```
predictLayout(draggedId, insertionIndex):
    rest = cards.filter { it != dragged }              // 1. 拖拽卡移出布局流
    rest.add(insertionIndex, dragged)                  // 2. 插到预测位置
    return flowPack(rest)                              // 3. 重新打包成网格矩形
```

`flowPack`（行填充打包，确定性）：

```
flowPack(orderedCards, columns = 2, spacing, width):
    x = 0; y = 0; rowHeight = 0
    for card in orderedCards:
        if x + card.gridWidth > columns:               // 当前行放不下 → 换行
            x = 0; y += rowHeight + spacing; rowHeight = 0
        card.targetRect = Rect(x, y, card.gridWidth × cellW, card.gridHeight × cellH)
        x += card.gridWidth + spacing
        rowHeight = max(rowHeight, card.gridHeight × cellH)
    return cards 的 targetRect 映射
```

性质（这些性质是选择插入模型而非交换模型的根本原因）：

1. **永不重叠**：flowPack 产出的矩形天然不重叠，天然满足「大卡碰撞找最近可用空间」。
2. **确定性**：同一 order 序列永远产出同一布局 → 位置可以从 order 派生，无需持久化 row/column。
3. **Wide(2 列) 自动换行**、未来 2×2 卡（`gridHeight=2` 已预留）零改动兼容。
4. 连续拖动 A 经过 B C D：每次命中核心区 → `predictLayout` 得新 order → 其余卡片 targetRect 变化 → spring 驱动 translation 到新槽位。视觉上就是 B C D 依次让位，实现需求第五节的连续重排。

### 5.2 每帧重排流程（Dragging 中）

```
maybeReorder(dragCenter):                     // 已被 4.1 的消抖节流
    if !shouldSwap(dragCenter): return
    index  = insertionIndexFor(dragCenter)     // 占位符目标行的行首索引
    layout = predictLayout(draggedId, index)
    temporaryLayout = layout
    placeholder.targetRect = layout[draggedId].targetRect   // 占位符随预测落点移动
    for card in layout:
        if card != dragged:
            card.animateTranslationTo(                        // 可取消 spring，见 7.7
                delta = card.targetRect.topLeft - card.layoutSlot.topLeft,
                spec = reorderSpring
            )
```

`insertionIndexFor(center)`：找占位符预测行 = 「center.y 落在哪个已打包行的 y 区间（含相邻行边界 50% 归属）」→ 该行行首的 order 索引。这比逐卡求最近中心更稳定（两列小卡逐卡判定会在 B/C 之间高频抖动，按行判定只有离散的行跳变）。

### 5.3 占位符（Ghost）

- 尺寸 = 拖拽卡当前槽位尺寸（含跨度），位置 = `temporaryLayout` 中拖拽卡的 targetRect。
- 视觉：`alpha 0.3~0.4` + `2dp 虚线描边`（品牌蓝 30% 透明度）+ 圆角与卡片一致。
- 移动动画与让位卡片同参数 spring，形成「队列整体流动」感。
- 松手后：拖拽卡 snap 到占位符位置的同时占位符 alpha → 0，150ms。

---

## 六、动画参数建议

### 6.1 编辑模式进入/退出（200~300ms，ease-out）

| 元素 | 动画 | 参数 |
|---|---|---|
| 页面整体 | scale 1 → 0.97 → 1（轻按一下再回弹） | 260ms，`EaseOutBack` 微过冲或纯 `EaseOut` |
| 背景点阵 | alpha 0 → 1 | 200ms ease-out，与页面动画同时开始 |
| 卡片 | 依次 stagger 浮动 + scale 1 → 1.01 | 每卡延迟 8ms，首尾总时长 ≤ 300ms |
| 卡片边缘 | 编辑描边 alpha 0 → 1 | 200ms |
| 「完成」按钮 | 从顶部滑入 | 220ms ease-out |
| 退出 | 全部反向 | 220ms ease-in-out，点阵渐隐先完成 |

### 6.2 交互动画总表

| 动作 | 曲线/弹簧 | 参数 |
|---|---|---|
| 拖拽跟手 | **线性、零延迟** | 每帧直接 set translation，不做插值（插值 = 手感迟滞） |
| 浮起 Lifted | spring | dampingRatio 0.7, stiffness 400（Compose）/ mass 0.5, stiffness 300, damping 30（通用 spring 公式） |
| 让位重排 Displacing | spring | **stiffness 300, damping 30, mass 0.5**（需求指定；Compose 对应 `spring(dampingRatio = 0.8f, stiffness = 300f)`，约等于轻微过冲，推开有「挤压回弹」感） |
| 释放吸附 Drop | ease-out snap | 180~220ms `FastOutSlowIn`；若位移 < 半格直接 120ms |
| 高速甩动落位 | ease-out + 速度衔接 | 起始速度 = 松手速度（避免速度突变为 0 的「撞墙感」） |
| 取消（原地松手） | spring 回原位 | stiffness 400, damping 30 |
| 占位符消隐 | alpha | 150ms ease-out |

### 6.3 物理感清单

- 拖拽中卡片 scale 保持 1.05，**不要**在移动中缩放（缩放属于拿起/放下时刻的语义）。
- 阴影随拖拽高度隐喻：Lifted → elevation 中，Dragging → 最大 + 阴影 y 偏移 4dp（光源感）。
- 所有 spring 共用同一个「时钟」，一次 `predictLayout` 引起的所有让位同时启动——分批启动会出现「波浪迟滞」。

---

## 七、边界情况处理方案

### 7.1 屏幕边缘自动滚动

```
edgeAutoScroll(dragCenterY, viewport):
    topZone    = 80px; bottomZone = 80px
    if dragCenterY < topZone:
        t = (topZone - dragCenterY) / topZone            // 0~1
        speed = lerp(200, 1400, t * t) px/s              // 距离平方加速
        scrollBy(-speed × frameDt)
    else if dragCenterY > viewport.bottom - bottomZone:
        对称向下
```

- 滚动期间**持续重算** dragCenter 相对布局区域的坐标并重跑 `maybeReorder`（卡片在视口下方向上滚动进入是常见路径）。
- 拖拽卡片本身坐标要随 scroll 偏移补偿（手指不动、内容滚动时，卡片相对布局区域的槽位在变，占位符预测也随之更新）。
- Compose 落地：`LaunchedEffect` 内循环 `listState.scrollBy()`，速度作为 State 避免重启协程。

### 7.2 拖出有效区域

- 拖拽中心 clamp 在 `[minX, maxX] × [minY, maxY]`（布局区域 = 首行卡片顶到最后卡片底 + 一行 buffer）。
- clamp 时手指与卡片分离是**正常且正确**的（iOS 同款行为）：卡片停在边缘，手指出去多远都不跟。
- minY 特别处理：不允许拖到状态栏/标题栏之下，minY = 标题栏底 + 8dp。

### 7.3 大尺寸卡片碰撞（Wide / 未来 2×2）

由 flowPack 天然解决：拖拽卡插入某 index 后重打包，Wide 卡放不下自动换行，其余卡片被推到下一行——**不存在覆盖态，也就不需要专门的碰撞解算**。占位符始终是合法布局中的一个槽位。

### 7.4 空白区域释放

目标行为已包含：`insertionIndexFor` 允许 index = n（列表尾部空白区），预测布局即「追加到最后」，占位符显示在末尾新行，松手直接占据。若松手点超出布局区域（clamp 边界上），吸附到最近合法 cell 的规则接管。

### 7.5 快速移动（甩动防抖）

三层，全部在 4.1/3.3 中实现：

1. **时间消抖**：swap 判定间隔 ≥ 50ms。
2. **空间消抖**：核心区连续 2 帧命中才触发。
3. **速度外推**：松手时用最近 100ms 速度 × 120ms 外推落点，`flingThreshold ≈ 1200px/s`。拖动中不做速度外推（会跟手滞后），只在释放瞬间用。

### 7.6 多指触控

```
dragSession.pointerId 绑定首次抓取的手指；
后续 onMove 只处理相同 pointerId 的位移事件；
其他手指的 down/move 全部忽略（不取消当前会话）；
绑定的手指 up/cancel → 正常走 Drop / 回弹。
```

### 7.7 动画冲突（取消不叠加）

```
animateTranslationTo(card, delta, spec):
    card.job?.cancel()                    // 取消旧动画，Animatable 会停在当前值
    card.job = scope.launch {
        card.translationX.snapTo(current) // 顺滑衔接：从当前位置继续
        card.translationX.animateTo(delta.x, spec)
        ...
    }
```

- 每个 `translationX/Y` 是独立 `Animatable`，`cancel()` 不清零值，新 `animateTo` 从当前值起跳 → 永不跳变。
- 拖拽卡自己绝不用 spring 跟手（只有 Drop 之后才用动画），从根上避免「拖拽 vs spring 互相打架」。

### 7.8 其他必查项

| 场景 | 处理 |
|---|---|
| 编辑态中列表数据变化（Room Flow 推送新事件） | 编辑会话期间冻结快照（`snapshotFlow` 缓存），退出时 diff 合并，新卡 append 到尾部 |
| 编辑态中系统返回键/后台 | 返回键 = 退出编辑（同「完成」）；onStop 时 commit 当前 temporaryLayout，防止丢布局 |
| 旋转/折叠屏/字体缩放 | 编辑会话直接取消回弹（不尝试跨配置迁移），重新进入 |
| 无障碍 | 编辑模式入口、完成按钮、拖拽卡片均有语义；提供「上移/下移」替代操作（后续版本） |

---

## 八、前端实现架构建议（Compose 落地）

### 8.1 分层

```
ui/home/edit/
├── EditableBoardState.kt        // 状态机 + DragSession + temporaryLayout（纯 Kotlin，可单测）
├── EditableBoardController.kt   // 手势 → 状态机输入；管理 Animatable 池、自动滚动协程
├── EditableBoardLayout.kt       // 自定义 Layout：flowPack 摆放 + per-card translation
├── BoardDotBackground.kt        // 背景点阵（Canvas，编辑态才组合）
├── DragOverlay.kt               // 拖拽卡片的浮层（Box + graphicsLayer）
└── BoardAnimations.kt           // 全部 AnimationSpec 常量（第六节的表落在这里）
```

### 8.2 关键架构决策

1. **编辑态用自定义 `Layout`，浏览态保持 `LazyVerticalGrid`**。两个模式切换组合（而不是把 LazyGrid 改造成支持拖拽——那会在 50+ 卡时把问题复杂化一个数量级）。编辑态数据是冻结快照（见 7.8），卡片数量 = 当前过滤结果，自定义 Layout 直接 measure 全部（编辑态禁用懒加载是可接受的：编辑时用户不会滑到 300 张之外，且可限制编辑范围 = 首屏 + 2 屏）。
2. **拖拽卡渲染在 overlay 层**：一个全屏 `Box`，`matchParentSize`，卡片内容复用现有 renderer（`EventCardFeature` 等），只包一层 `graphicsLayer { translationX/Y; scaleX/Y; shadowElevation }`。原槽位由 Ghost 占位。
3. **单向数据流**：手势 → `EditableBoardState`（状态机输入）→ 新 `BoardUiState` → Compose 渲染。状态机不知道 Compose，Compose 不知道指针细节。
4. **让位动画的重组隔离**：每张卡的 translation 放在 `Modifier.graphicsLayer { translationY = cardOffset.value }`，`cardOffset` 是 `Animatable<Float>` 且**只在 lambda 内读取** —— 动画每帧只重绘图形层，不触发重组（Compose performance 三定律：少重组、少重测、图形层扛动画）。
5. **背景点阵**：`Canvas` 绘制，间距 = 卡片吸附单位（`cellW`），密度 = `columns × rows` 由 `BoxWithConstraints` 派生；`pointerInput` 不挂在它上面，纯装饰层，不消费事件。点阵颜色 = `onSurface.copy(alpha = 0.12)`（暗色主题 `surfaceVariant` 0.15）。
6. **动画规格统一**：第六节所有参数收敛到 `BoardAnimations.kt` 常量对象，禁止散落 magic number。

### 8.3 性能预算（50+ 卡 / 60fps）

| 项 | 预算 | 手段 |
|---|---|---|
| 拖拽帧 | < 4ms UI 线程 | translation 直写 graphicsLayer；swap 判定 O(1)（空间索引） |
| 重排帧 | < 8ms | 一次 predictLayout 最多触发全部卡片 spring，但动画在 RenderThread 合成；measure 不重跑（只改 translation） |
| 重组范围 | 仅动画卡片 | 状态下沉 + lambda 读状态 |
| 内存 | 每卡 2 个 Animatable | 编辑退出时全部释放 |

---

## 九、数据层设计

### 9.1 模型（向后兼容现有 `HomeCardLayout`）

```kotlin
// 持久化（DataStore，沿用现有结构，不改版本）
HomeCardLayout(cardId, order, gridWidth, gridHeight)   // 保持不变

// 内存态（仅编辑会话存活）
data class BoardSession(
    val layout: List<HomeCardLayout>,          // temporaryLayout，拖动中实时更新
    val snapshot: List<HomeCardLayout>,         // 进入编辑时的冻结快照（退出恢复用）
    val undoStack: ArrayDeque<List<HomeCardLayout>>,  // 上限 50
    val redoStack: ArrayDeque<List<HomeCardLayout>>,
)
```

`row/column` **不持久化**：由 `flowPack(order)` 派生（见 5.1 性质 2）。这保证旧版本数据、被筛选隐藏的卡片重新出现时，布局依然合法稳定——正是现有 `reorderedCardLayouts` 注释里「dense and stable」语义的延续。

### 9.2 生命周期

```
进入编辑  → snapshot = 当前 DataStore Flow 值（冻结）
拖动中    → layout = temporaryLayout（纯内存，不落盘）
每次 Drop → undoStack.push(layout)；redoStack.clear()；layout = 新 order
点击完成  → CardLayoutStorage.saveCardLayout(layout)（复用现有持久化）
撤销      → layout = undoStack.pop() → redoStack.push；saveCardLayout
系统回收  → onStop 时若 layout != snapshot，静默 save（宁可保存也不丢）
```

### 9.3 undo/redo 交互

- 编辑态内：「完成」旁提供撤销按钮（长按显示重做）。
- 退出后：Snackbar「布局已保存」+ 撤销动作（复用现有删除 Snackbar 模式，一次 Drop = 一个撤销单元）。

---

## 十、验收清单

- [ ] 进入/退出编辑模式动画 260ms ±，点阵渐显渐隐，非编辑态完全无点阵、无 graphicsLayer 开销
- [ ] 长按 150ms 浮起 + haptic，scale 1.05，占位符出现
- [ ] 拖动跟手无插值延迟；其他卡片 spring 让位（stiffness 300 / damping 30 / mass 0.5）
- [ ] 拖 A 连续经过 B C D，实时得到 [B][C][D][A]，无等待松手
- [ ] Wide 卡与 Small 卡混合，任何拖动结果无重叠、无非法布局
- [ ] 松手吸附（阈值 = 卡宽 30%）；高速甩动速度外推落位
- [ ] 顶部/底部 80px 自动滚动，滚动中重排持续有效
- [ ] 拖出边界 clamp，卡片不越过标题栏/底栏
- [ ] 双指同时触摸，第二个手指完全被忽略
- [ ] 快速抖动手指不触发高频交换（swap ≥ 50ms 间隔 + 连续 2 帧命中）
- [ ] 编辑中按返回 = 完成退出；后台被回收后布局不丢
- [ ] undo/redo 生效，粒度 = 每次 Drop
- [ ] 50 卡模拟数据下，Android Studio Frame Metrics 无掉帧（UI 线程 < 8ms）
- [ ] 暗色主题、200% 字体缩放下点阵与占位符可读性正常

---

## 十一、实施切分建议

| 阶段 | 内容 | 风险 |
|---|---|---|
| P1 | 状态机 + 编辑态视觉（点阵/浮动/描边/完成按钮），拖拽沿用现有松手交换 | 低 |
| P2 | overlay 拖拽 + 占位符 + 插入式预测重排 + spring 让位（核心体验） | 中 |
| P3 | 吸附/速度外推 + 边缘自动滚动 + 消抖全套 | 中 |
| P4 | undo/redo + 冻结快照合并 + 后台保存 + 无障碍替代操作 | 低 |

P2 完成即达到「体验质变点」，P3/P4 是打磨项。
