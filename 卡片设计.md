你需要在当前手机 App 项目中新增一个「时间倒计时卡片（Countdown Card）」组件。

我会同时提供：

1. 当前 App 首页界面截图（用于理解现有 UI、布局、卡片体系）
2. 倒计时卡片设计参考图（用于理解目标视觉方向）

你的任务不是单独制作一张展示图片，而是使用代码实现一个真正可以运行、可以放入当前 App 首页的卡片组件。

最终目标：

让这张倒计时卡片看起来像这个 App 原生自带的功能，而不是后来插入的一张第三方组件。

---

# 一、开始开发前必须先分析现有项目

请先阅读当前代码结构，找到：

* 首页页面
* 卡片列表/Grid布局
* 当前 Card Component
* 卡片数据模型
* 卡片尺寸计算方式
* 主题系统
* 颜色系统
* 字体规范
* 阴影规范
* 圆角规范
* 图片加载方式
* Icon 系统
* 卡片点击事件
* 编辑/删除/排序逻辑

不要直接创建一个新的独立 Card 样式。

如果已有：

* BaseCard
* WidgetCard
* DashboardCard
* ComponentCard
* GridItem

等基础组件，请优先继承或扩展。

---

# 二、设计目标

根据提供的参考图，实现一张高级感的倒计时卡片。

视觉关键词：

* 极简
* 高级
* 舒服
* iOS Widget 风格
* 柔和渐变
* 圆角
* 图片背景
* 半透明层
* 清晰的信息层级

但不要机械复制参考图。

需要把参考设计语言转换成当前 App 风格。

优先级：

当前 App Design System > 参考图片风格

---

# 三、必须适配当前主页卡片布局

当前主页采用卡片排列模式。

假设：

每行两个普通卡片。

例如：

```
| Card A | Card B |

| Card C | Card D |
```

新的 Countdown Card 必须支持：

---

## 模式 1：普通尺寸 Small Card

占据一个卡片位置：

```
| Countdown | Card B |
```

要求：

* 宽度 = 当前普通卡片宽度
* 高度 = 当前普通卡片高度
* margin/padding 完全一致
* 圆角完全一致
* 阴影完全一致

不能自己定义尺寸。

必须跟随现有 Grid。

---

## 模式 2：宽卡片 Wide Card

支持占据两列：

```
|        Countdown Card        |
```

要求：

宽度：

= 两张普通卡片宽度 + 中间间距

高度：

根据当前系统规则决定。

不要简单把 Small Card 拉宽。

Wide 模式需要重新调整内部布局。

---

如果当前项目有：

* columnSpan
* gridSpan
* size
* widgetSize

类似机制，请直接使用。

---

# 四、卡片内容结构

卡片应该包含：

## 顶部区域

Category / Icon

例如：

```
📅 纪念日
```

---

## 主标题

例如：

```
东京旅行
```

---

## 核心数字

例如：

```
28 天
```

数字必须是视觉中心。

要求：

* 最大字号
* 高视觉权重
* 自动适配数字长度

例如：

```
9 天

28 天

365 天

1024 天
```

都不能破坏布局。

---

## 日期信息

例如：

```
2026.01.15 星期四
```

---

# 五、所有内容必须支持用户自定义

不要写死 Demo 数据。

需要设计成数据驱动。

例如：

```typescript
CountdownCardData {

 id

 title

 subtitle

 category

 targetDate

 icon

 background

 theme

 size

}
```

具体字段根据项目现有架构调整。

当前 Android 实现对应为 `TravelCardConfig`：

```kotlin
TravelCardConfig(
    badgeLabel, title, groupLabel, countdownUnit,
    targetDate, locationLabel, backgroundPreset,
    badgeIcon, dateIcon, size
)
```

模板不直接读取 Demo 文案；首页通过事件配置渲染，事件设置页可实时修改标题、标签、分组、目标日期、顶部 icon 和卡片尺寸。

当前模板键还包括：

- `TRAVEL_MINIMAL_EDITORIAL`：奶油极简日系编辑风
- `TRAVEL_SUNSET_GLASS`：上一版黄昏玻璃风
- `TRAVEL_SCRAPBOOK`：既有旅行模板

新增模板只扩展模板枚举和渲染分支，不删除既有卡片。

---

用户未来可以修改：

## 标题

例如：

东京旅行

生日

毕业

考试

恋爱纪念日

---

## 时间

用户可以选择：

* 日期
* 时间

倒计时数字必须动态计算。

不要：

```
countdown = 28
```

应该：

```
countdown = targetDate - currentDate
```

---

## Icon

支持替换：

* 系统 Icon
* Emoji
* 内置图标
* 用户上传图片

---

## 背景

支持：

1. 图片背景

例如：

旅行照片

风景照片

人物照片

---

2. 渐变背景

---

3. 纯色背景

---

背景处理需要考虑：

* 图片裁剪
* Aspect Fill
* 模糊
* 遮罩
* 文字可读性

---

# 六、设计细节要求

请严格控制：

## 对齐

重点检查：

* 左右 padding
* 顶部间距
* 元素 baseline
* 数字中心位置
* 日期位置

---

## 圆角

必须跟当前 App 卡片一致。

不要重新定义。

---

## 间距

必须：

```
Card spacing
=
Existing Card spacing
```

不要产生视觉错位。

---

## 字体

遵循当前 App：

* Font family
* Weight
* Size hierarchy

---

# 七、小尺寸和大尺寸分别设计

不要：

Small Card:

```
缩小版 Wide Card
```

Wide Card:

```
放大的 Small Card
```

应该：

---

Small:

强调：

快速查看

信息密度

例如：

```
纪念日

东京旅行

28 天

2026.01.15
```

---

Wide:

强调：

视觉体验

背景图片

大数字

例如：

左侧：

```
东京旅行
2026.01.15
```

右侧：

```
28
天
```

---

# 八、兼容现有卡片功能

新卡片必须继承已有能力。

如果现有卡片支持：

* 点击
* 长按
* 编辑
* 删除
* 拖动排序
* 收藏
* 固定
* 菜单操作

Countdown Card 必须兼容。

不要创建一个无法管理的新类型。

---

# 九、主页展示要求

开发完成后：

不要只创建 Preview 页面。

请直接放入当前主页。

让我可以看到真实效果：

例如：

当前：

```
Card A | Card B

Card C | Card D
```

替换：

```
Countdown | Card B

Card C | Card D
```

同时展示：

Small:

```
Countdown
```

Wide:

```
Countdown Countdown
```

方便比较。

---

# 十、动画

可以加入轻量动画：

例如：

* 卡片出现动画
* 数字变化动画
* 点击缩放

但是：

禁止复杂动画。

保持：

高级、克制。

---

# 十一、代码要求

不要：

* 大量 absolute positioning
* 写死 px
* 新建无必要依赖
* 修改无关页面
* 破坏已有 Card

优先：

* 使用现有组件
* 使用现有 Theme
* 使用现有 Layout

---

# 十二、验收标准

完成后必须满足：

✅ 可以显示在当前主页

✅ Small 尺寸完全适配现有卡片

✅ Wide 尺寸完全适配两列宽度

✅ 与现有卡片视觉统一

✅ 支持动态倒计时

✅ 标题可替换

✅ 日期可替换

✅ Icon 可替换

✅ 背景图可替换

✅ 不破坏已有卡片功能

✅ 不同数字长度不会错乱

✅ 长标题不会破坏布局

✅ 不同手机尺寸正常显示

✅ 看起来像 App 原生功能

---

# Demo 数据

初始展示：

标题：

东京旅行

分类：

纪念日

日期：

2026-01-15

Icon：

旅行 / 日历

背景：

东京旅行氛围图片

---

最后：

请先理解当前 App 的设计系统，再实现 Countdown Card。

不要为了还原参考图片而牺牲 App 整体一致性。

最终目标：

「一个强迫症用户放在主页上，看起来完全舒服、自然、统一的倒计时卡片。」
