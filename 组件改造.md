# `design.html` 组件改造建议

> 分析日期：2026-09-07  
> 分析对象：仓库根目录 [`design.html`](design.html)（11 屏主稿 + 夜间暂存区）  
> 决策依据：`PRD.md` → `docs/design/proposal_J_final.html` → `DESIGN_SYSTEM.md` → `ARCHITECTURE.md` → 当前 Compose 代码。

## 1. 结论先行

`design.html` 值得抽成组件的内容，不是每一块带边框的 HTML，而是以下几组稳定的“视觉 + 交互契约”：

1. 像素材质基础：面板层级、按钮、输入框、标签、徽章、血条、经验格、星级、图标槽。
2. 页面骨架：状态栏/页面标题/返回按钮、滚动表单、固定三入口底栏。
3. 业务展示：统一的 Moment 卡片、Task 行、日历日格、成就里程碑、回收站条目。
4. 生命周期交互：封印确认、撤销 Toast、复活/净化动作、设置行。

组件应接收已经由 ViewModel/Domain 派生好的 presentation state，不接收 Room Entity 后自行计算天数、排序或决定 Moment/Task 身份。视觉值全部来自 `MomentMarkTokens` 和 `MaterialTheme`；组件只负责呈现和回调事件。

当前代码已经有 `PixelPanel`、`PixelTextInput`、`PixelHpBar`、`AchievementFrame`、`PageTitlePanel`、`PixelBottomNav`、`PixelConfirmationDialog`，应继续作为基础层。优先把各屏里的 `private @Composable` 提升为可测试的共享组件，不要先重写现有卡片或恢复旧 Hero/模板墙。

### 源稿差异提醒

- 根目录 `design.html` 标题为 11 屏；`docs/design/proposal_J_final.html` 仍是 13 屏历史稿。本文按用户点名的根目录 `design.html` 分析，产品行为仍以 PRD/J 优先级为准。
- `design.html` 把 ⑤日子簿、⑨设置、⑩回收站的主稿标为“昼”，并在底部另存夜间暂存；当前 `MomentMarkApp.kt` 仍有把日子簿上下文强制为洞窟主题的代码。这个是主题策略差异，不应通过复制组件解决，落地前需以产品决定统一昼/夜。
- `design.html` 的 ④任务表单没有画出“今日/明日/本周六/自选”快捷日期，但 PRD P0 和当前 `TaskFormScreen` 都要求它。组件改造应保留 `QuickDateButton` 能力，不能因稿面省略而删除。

## 2. `design.html` 屏幕与复用关系

| 屏幕 | 设计内容 | 可复用组成 | 产品边界 |
|---|---|---|---|
| ① | 首页「大事件」：主线宽卡、置顶成就、双列小卡 | `HomeMainQuestCard`、`HomeAchievementCard`、统一 `HomeMomentCard`、`PixelHpBar`、图标槽、元数据行 | P0；Task 只能作为明确开启后的右列投影 |
| ②③ | 铭刻时刻：未来/过去两种方向 | `PixelPageHeader`、`MomentDirectionSelector`、`PixelDateSelector`、`PixelTextInput`、`GroupChipRail`、`PixelStarRating`、方向 CTA | P0；未来/过去共享一套表单，字段文案和颜色由方向 state 切换 |
| ④ | 接受新任务 | `PixelPageHeader`、快捷日期、名称/备注输入、截止日期时间、难度星级、提交 CTA | P0；只能从日子簿进入，不能产生 Moment |
| ⑤ | 日子簿：三个月横滑日历、今日掉落、完成任务行 | `MonthPager`、`CalendarDayCell`、`TaskRow`、`TaskCheckbox`、`NewQuestEntry` | P0；跨日只改变查询结果，不迁移任务 |
| ⑥ | 任务详情：摘要、LORE、前置目标、DUE/GROUP/RARITY | 视觉上可复用详情摘要/元数据/目标行 | 任务详情与前置目标不是当前 P0 数据契约；先保留规划，不做无行为组件 |
| ⑦ | 成就详情：金框摘要、STORY、固定里程碑、操作 | `AchievementFrame`、`AchievementSummary`、`MilestonePanel/Row`、操作按钮组 | 本轮追加 P0；里程碑必须由 `anchorDate + Clock` 派生，只读 |
| ⑧ | 分类抽屉 + 搜索 | `SearchField`、`FilterDrawer`、状态徽章、分组筛选项 | P1；未授权前不接入主路由，不给空 `onClick` |
| ⑨ | 设置：冒险者卡、显示模式、设置行 | `PlayerProfileCard`、`XpBar`、`SettingsRow/Section` | P0 回收站/自动净化；完整个人等级系统不是当前业务事实 |
| ⑩ | 封印之地：条目、复活、永久净化、清空 | `RecycleBinItemCard`、危险/奖励按钮、确认弹窗 | P0；保留原始字段和状态，永久操作二次确认 |
| ⑪ | 删除确认 + 撤销 | `PixelConfirmationDialog`、`PixelUndoToast` | P0；与 Moment/Task 共用生命周期，文案按类型变化 |

夜间暂存区（`design.html:817-825`）是同一页面的主题变体，不是三条新路由。昼/夜应由主题 token 和页面上下文切换，不能复制出 `DaybookNightScreen`、`SettingsNightScreen` 等平行页面。

## 3. 组件分层与建议

### 3.1 基础视觉层（优先级 P0，`ui/components/foundation`）

| 建议组件 | 当前状态 | 主要 API/变体 | 说明 |
|---|---|---|---|
| `PixelPanel` | 已有 | `Raised` / `Emphasized` / `Flat` | 对应 `.px`/`.px4`；普通卡 3dp 硬影，主卡/弹窗 4dp。禁止在业务组件内重复写 border/shadow。 |
| `PixelButton` | 建议新增 | `Primary`(绿)、`Secondary`(蓝)、`Reward`(金)、`Danger`(红)、`Ghost` | 将多处 `PixelPanel + clickable + Text` 统一；内置按压位移、44dp 触摸目标、Role 和 enabled/loading 状态。 |
| `PixelPageHeader` | 建议扩展 `PageTitlePanel` | `title`、`hud`、可选 `onBack` | `design.html:55-59,95` 反复出现。返回按钮视觉尺寸可为 25dp，但外部触摸容器必须达到 `TouchTargetMin`。 |
| `PixelTextInput` | 已有 | 单行/多行、错误、placeholder | 保留现有方块光标和 selection 语义；`FormField`/`TaskField` 只做薄适配，不再复制输入框布局。 |
| `PixelFieldLabel` | 建议新增 | `label`、`helper`、`tone` | 统一 `fieldlabel` 的 mono 字阶、字距和辅助说明；不把“LORE/STORY”等业务事实写死在基础组件。 |
| `PixelBadge` | 建议新增 | `Main`/`Side`/`Limited`/`Achievement`/自定义 token 色 | 对应 `.badge-tag` 和首页/详情 HUD；统一 2dp×6dp 留白和边框。 |
| `PixelStarRating` | 建议新增 | `value: Int?`、`max=3`、`interactive` | Moment 重要度与 Task 难度共用绘制层；由外层决定 label（RARITY/DIFFICULTY）和是否可编辑。 |
| `PixelHpBar` | 已有 | `fraction`、可选 `fillColor` | 颜色和比例只接受派生值；不要在卡片里再实现阈值或绘制矩形。 |
| `PixelXpBar` | 建议新增 | `filledCells`、`cellCount`、`tone` | `design.html:70-72` 的 16 格经验条与设置页头像卡复用；这是视觉进度组件，不等价于 Moment 的时间血条。 |
| `PixelIconSlot` | 建议新增 | 固定槽尺寸、`iconKey`、装饰/语义模式 | 首页小卡右上角固定槽。图标由 `Group.iconKey` 解析，无分组使用默认图标，图标变化不得改变标题宽度和网格对称性。 |
| `PixelDashedDivider` | 建议新增 | `orientation`、颜色 token | 日历任务行、设置行、里程碑行、回收站说明反复使用；统一虚线节奏，不在页面里散落 `PathEffect`。 |

### 3.2 表单层（P0，放在 `ui/moment` / `ui/task` 的共享子包）

| 建议组件 | 来源/当前代码 | 边界 |
|---|---|---|
| `MomentDirectionSelector` | `MomentFormScreen.kt` 的 `DirectionChoice`；对应屏幕 ②③ | 两个等宽直角块，未来蓝/过去金；只回调 `MomentDirection`，不保存或计算日期。 |
| `PixelDateSelector` | `DateTripleSelector` + `DatePartDropdown`；对应 `design.html:291-299,354-362` | 年/月/日三个独立选择格，父容器 `weight` 布局，固定 `[x] 全天`；输出一个 `LocalDate` 事实值。必须按方向约束可选范围。 |
| `TaskDueDateTimeField` | 设计屏幕 ④ 的日期/时间双框 | Task 的 `dueLocalDate` 与可选 `dueInstant/zoneId` 分开；日期决定日子簿归属，时间只用于展示/提醒。不要复用 Moment 的三格日期语义。 |
| `GroupChip` + `GroupChipRail` | `MomentFormScreen.kt` 的 `GroupChip`/`GroupSelector`；对应 `party-rail` | 支持选中/未选中、线路色、内置图标、横向溢出；输出稳定 `groupId`，不能把组名或颜色复制进 Moment/Task。 |
| `PixelFormSection` | 各表单重复的 `PixelPanel + Column + label + content` | 只抽布局和间距；section 标题、校验与保存仍由表单 screen/ViewModel 管理，避免“万能表单 schema”掩盖产品边界。 |
| `PixelFormScaffold` | 两个表单均需边到边、IME、滚动 | 负责 `statusBarsPadding`、`imePadding`、滚动和内容宽度；不要自动附加底部导航。`design.html` 的表单没有底栏，符合叠加表单路由。 |

### 3.3 首页与详情层（P0，保持现有视觉）

| 建议组件 | 当前代码/改造方式 | 关键契约 |
|---|---|---|
| `HomeMomentCard` | 将 `MomentQuestCard` 与 `MomentAchievementCard` 的共同骨架抽出；保留 `HomeMainQuestCard`、`HomeAchievementCard` 作为语义包装 | 标题行、固定图标槽、数字行、血条、DUE/SINCE 元数据一致；过去/未来只由 presentation state 改变语义色和单位。不要重做现有卡片或引入旧模板。 |
| `HomeMainQuestCard` | 已有，继续复用 | 仅供置顶未来 Moment 的全宽主线卡，`Emphasized`；不承载 Task。 |
| `HomeAchievementCard` / `AchievementFrame` | 已有，继续复用 | 仅供过去成就语义；里程碑进度必须是真实 `MilestoneCalculator` 结果。 |
| `HomeTaskCard` | 已有，继续复用 | 只读首页投影；不能点击进入 Moment 详情，编辑/完成回到日子簿。 |
| `MomentMetaRow` + `MomentBadge` | 当前在 `MomentCards.kt` 中为私有函数 | 提升为 feature 内共享函数/组件，统一日期格式、分组显示和徽章；输入应是 presentation，不接收 Room。 |
| `MomentSummaryPanel` | 当前在 `MomentDetailScreen.kt` 中为私有函数 | 统一未来 QUEST/过去 ACHIEVEMENT 摘要；外层选择 `PixelPanel` 或 `AchievementFrame`，内容顺序保持设计稿。 |
| `MomentStoryPanel` | 当前私有函数；对应 STORY/LORE | 只在 note 非空时渲染；label 随派生展示状态切换，正文不改写。 |
| `MilestonePanel` + `MilestoneRow` | 当前私有函数；对应屏幕 ⑦ | 行是只读状态：已达成/下一项/远征中。当前项显示金色时间条；禁止可点击复选框、手工完成或自定义节点。 |

### 3.4 日历与任务层（P0）

| 建议组件 | 当前状态 | 说明 |
|---|---|---|
| `MonthPager` | 当前只有 `MonthGrid`，设计稿增加三个月横滑（`design.html:129-134,445-469`） | 封装月份页、左右按钮、横向 swipe/snap 和当前月份 label；月份切换仍由 `DaybookViewModel` 驱动，组件不调用 `LocalDate.now()`。 |
| `CalendarDayCell` | `DayCell` 当前为私有 | 输入 `date/inVisibleMonth/selected/today/hasTasks`，输出日期点击；统一普通、今天、选中、其他月份、有任务五种视觉与中文语义。 |
| `DaybookDropPanel` | 当前在 `DaybookScreen` 内联 | 负责标题「今日掉落」、空态、加载态、任务列表和新任务入口；任务查询与排序仍在 ViewModel。 |
| `PixelTaskRow` | 当前 `TaskRow` 私有 | 将 checkbox、标题删除线、截止信息、`CLEAR +5 EXP`、编辑/封印动作封装；必须同时承载完整中文 content description。 |
| `PixelTaskCheckbox` | 当前 `TaskCheckbox` 私有 | 视觉盒 15dp，外层触摸目标至少 44dp；Role.Checkbox，装饰勾选符号不重复读屏。 |
| `NewQuestEntry` | 当前 `NewQuestEntryRow` 私有 | 仅在日子簿列表末尾出现，绿色虚线新任务入口；不得出现在全局新时刻表单或首页。 |

### 3.5 设置、回收站与生命周期层（P0）

| 建议组件 | 当前代码/来源 | 说明 |
|---|---|---|
| `PlayerProfileCard` | `design.html:684-699` 尚未有对应共享组件 | 仅展示已有用户资料/可验证 EXP；不要把 LV、连续打卡、金币做成未落库的业务状态。 |
| `SettingsSection` + `SettingsRow` | `SystemSettingsScreen.kt` 已有私有 `SettingsRow/SettingsListRow` | 统一行高、虚线分隔、trailing 文案和中文语义；可用于回收站、自动净化、主题入口。 |
| `GroupManagementCard` | 当前 `GroupManagementPanel` 是整页私有实现 | 组卡应展示图标、线路色、Moment/Task 活动计数、改名/解散；解散确认和跨表事务由 ViewModel/Repository 负责。 |
| `RecycleBinItemCard` | 当前 `RecycleBinRow` 是私有 | 输入 `RecycleBinItemPresentation`（类型、封存时间、剩余天数）；复活和净化只通过回调。不要在组件中用 `Clock` 计算剩余时间。 |
| `PixelConfirmationDialog` | 已有 | 统一封印/永久净化/清空/解散确认；继续保持直角像素面板和危险确认按钮。 |
| `PixelUndoToast` | 设计屏幕 ⑪ 有，当前未见独立共享组件 | 使用墨底金字、单一撤销回调和 Snackbar 生命周期；撤销事件要回到统一回收仓库流程。 |

## 4. 不应现在抽成运行组件的内容

| 内容 | 原因/处理 |
|---|---|
| `.phone`、`.page`、`.grid`、`.shot`、`.cap`、页首 `rules`、页尾 footer | 是 HTML 展示稿的画板/说明结构，不是 Android 产品组件。固定 330×664、圆角手机壳和 7px 外阴影不能迁入 Compose 页面。 |
| 示例标题、日期、`LV.07`、`GOLD ×1,280`、`EXP +148`、emoji | 是 mock 数据或装饰文案。组件应接收真实 presentation；无数据时显示明确空态，不伪造游戏状态。 |
| `light-preview` 与 `[style*=...]` 的主题覆盖 | 这是 HTML 为昼间主稿复用夜间 DOM 的预览技巧。Compose 应通过 theme/token 选择，不在组件内做字符串样式覆盖。 |
| 夜间暂存区 | 是主题样式候选，不增加导航入口或复制 screen。当前主稿以昼间为默认，日子簿上下文是否固定洞窟按现有 App 主题策略执行。 |
| ⑥ 前置目标/目标复选框 | PRD 将时刻与任务关联列为后续能力；当前 P0 不应创建 `Objective` 组件、数据表或空回调。 |
| ⑧ 搜索/筛选抽屉 | PRD 定为 P1。可以记录接口草案，但未获授权前不要接入首页路由、状态筛选或分组筛选。 |
| ⑦ 的周年提醒 | `design.html:388-389,617-618` 仍画出开关，但 PRD/DESIGN_SYSTEM 明确在通知链路交付前隐藏。不要抽成可点击设置组件。 |
| 装备库/模板浏览 | 属于 P2；旧模板池、自由卡片墙和拖拽布局已退出 P0。未来若重做，应使用新的 `MomentCardPresentation/TaskCardPresentation`，不能把旧模板字段塞回实体。 |

## 5. 数据、状态与无障碍边界

### 5.1 Presentation 输入

建议为共享组件定义窄的只读展示模型，例如：

```text
MomentCardPresentation
  title, anchorDate, displayState, daysLabel,
  accent, badge, groupIconKey, metaText,
  hpFraction, isPinned, nextMilestone

TaskRowPresentation
  title, dueLabel, isCompleted, taskType,
  groupLabel, canEdit, canSeal
```

`displayState`、天数、血条比例、下一里程碑和排序均由 `EventTimeCalculator`、`MilestoneCalculator`、`HomeMomentProjector` 或 ViewModel 派生；组件不得写入数据库，也不得以当前系统时间重新计算。

### 5.2 交互回调

- 卡片：`onClick(id)`；首页 Task 卡不提供 Moment 详情回调。
- 表单：字段变化回调 + `onSubmit`；保存/校验由 ViewModel 决定。
- 日历：`onDateSelected(LocalDate)`、`onMonthChanged(offset)`；不在 `DayCell` 内部持有业务日期。
- 生命周期：`onSealRequest`、`onRestore`、`onPermanentlyDelete`、`onUndo`；确认弹窗只负责确认，不直接操作 Repository。

### 5.3 无障碍与适配

- 卡片合并为完整中文语义节点，包含标题、日期、过去/未来状态、分组和完成状态；装饰 emoji/icon 隐藏或提供必要的图标语义。
- 视觉日期格、checkbox、返回图标可小于 44dp，但外层可点击边界必须达到 `TouchTargetMin`。
- 方向块、分组 chip、按钮显式声明 RadioButton/Button 角色和选中状态；底栏继续暴露 `selected=true/false`。
- 130%/200% 字体下不裁剪标题、主数字、`CLEAR`、日期和确认按钮；不要用固定手机画板尺寸推导 Compose 布局。

## 6. 建议改造顺序

### P0-A：先收敛基础层

1. 在现有 `PixelPanel`、`PixelTextInput`、`PixelHpBar`、`AchievementFrame` 上补 `PixelButton`、`PixelPageHeader`、`PixelBadge`、`PixelStarRating`、`PixelXpBar`、`PixelIconSlot`。
2. 将 `MomentFormScreen` 的方向块、日期三格、分组 chip、重要度星级改为公开可测试组件；保持当前字段顺序和 token。
3. 给基础组件补 Compose 语义/按压/大字体测试，不改变产品行为。

### P0-B：抽取日子簿与生命周期

1. 把 `DayCell`、`TaskRow`、`TaskCheckbox`、`NewQuestEntryRow` 提升为共享组件；再实现受 ViewModel 驱动的 `MonthPager`。
2. 把 `RecycleBinRow`、`SettingsRow` 和按钮动作统一到 `RecycleBinItemCard`、`SettingsRow`、`PixelButton`。
3. 把删除后的撤销消息收敛为 `PixelUndoToast`，继续使用现有软删除/恢复/净化仓库。

### P0-C：统一首页与成就详情

1. 保留 `HomeMainQuestCard`、`HomeAchievementCard`、`HomeTaskCard` 的外观，抽共享的标题/数字/图标槽/元数据内部布局。
2. 将 `MomentSummaryPanel`、`MomentStoryPanel`、`MilestonePanel` 提升为详情 feature 内可复用组件；里程碑继续只读派生。
3. 所有卡片和详情只接收 presentation state，并补未来/今天/次日过去转换、置顶排序和重启恢复测试。

### P1/P2：只做接口草案，不接入 P0

- P1：`SearchField`、`FilterDrawer`、Task 详情/前置目标、通知相关控件。
- P2：装备库/模板浏览、更多皮肤、自由排版。

## 7. 验收清单

- [ ] 每个共享组件的颜色、字号、间距、描边和硬影均能追溯到 `DESIGN_SYSTEM.md` token。
- [ ] `Moment` 与 `Task` 没有通过“通用卡片”互相伪装；组件参数显式区分两者。
- [ ] 组件不调用 Room、DataStore 或 `LocalDate.now()`；时间和排序由上层提供。
- [ ] 普通卡使用 3dp `Raised`，主卡/成就/弹窗使用 4dp `Emphasized`；不恢复旧 Hero、卡片墙或模板池。
- [ ] 日期三格、方向块、分组 chip、日历列和任务行使用父容器比例/weight，不把屏幕宽度写死。
- [ ] 关键触摸目标达到 44dp；完整中文语义可被 Compose 测试和 TalkBack 读取。
- [ ] 运行 `testDebugUnitTest assembleDebug lintDebug`；涉及日历、表单、回收站和 Room 时运行对应 `connectedDebugAndroidTest`，并分别报告模拟器、真机、TalkBack 和大字体未验证边界。

## 8. 最终建议

本轮最值得马上做的不是增加更多页面，而是把已有页面中的重复结构变成少量稳定的组件：

`PixelButton` → `PixelPageHeader` → `PixelDateSelector`/`GroupChipRail` → `CalendarDayCell`/`PixelTaskRow` → `RecycleBinItemCard`/`PixelUndoToast` → 首页卡片共享内部骨架。

这条路径能直接降低重复样式和无障碍漂移，同时保持 J 最终设计、Moment/Task 双体系和当前已验证的卡片外观。搜索抽屉、前置目标、周年提醒、装备库应继续停留在规划层，等对应产品能力和数据契约明确后再抽组件。
