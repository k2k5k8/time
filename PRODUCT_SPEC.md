# PRODUCT_SPEC

## 产品

- 产品名：刻间 / Moment Mark
- 定位：把重要事件变成始终可见的时间卡片。
- 核心表达：未来显示“还有多久”，过去显示“已经多久”；同一事件跨过目标时刻后自然切换。
- 首版本地优先、离线可用，不接账号、后端、广告或社交。
- App 是创建与管理入口，桌面 Widget 是后续最高频查看入口。

## 当前事实（2026-08-19）

阶段 1（含原阶段 1.5）已经完成可构建雏形，阶段 2 已完成模拟器冒烟，阶段 3/4/5/6 已建立并接入本地持久化，阶段 7 已开放删除撤销；当前进行阶段 7B 质量加固：

1. 首页暖白悬浮标题栏、分类入口、搜索图标和设置入口。
2. 左侧暖白抽屉中的 `全部 / 未来 / 过去 / 置顶` 与事件分组可组合筛选；数量从 Room 活动事件实时派生。
3. 主页固定两列：Small 占一格，Wide 跨两列。
4. 经典三段式蓝白/橙白卡，以及奶油极简、黄昏玻璃、旅行手账三种模板。
5. `TravelCardConfig` 支持标题、标签、分组、日期、icon 和 Small/Wide 的内存实时编辑。
6. 旅行模板和经典卡片均通过 `EventTimeCalculator` 读取派生时间；`ALL_DAY` 使用 `LocalDate`，`TIMED` 使用 `Instant`。
7. `Clock` 与 `ZoneId` 可注入；设置页提供跟随系统 / 浅色 / 深色三态，并由 DataStore 恢复与保存。
8. 搜索已实现当前内存样例的标题筛选；首页与日子簿共用同一套轻量胶囊时间轴导航，默认选中“大事件”。
9. Room v2 的事件实体、DAO、v1→v2 migration 和 Repository 已建立；Repository 写入校验 `ALL_DAY` / `TIMED` 的事实字段形状。
10. DataStore 已保存 `SYSTEM` / `LIGHT` / `DARK` 主题设置，并有 Android instrumentation tests 覆盖默认值与往返读写。
11. 首页首次启动会播种 4 个真实样例到 Room，随后通过 Flow 读取；模板画廊仍是内存预览，不会写入数据库。
12. 事件设置页现有模板和旅行字段编辑会写回 Repository，重启后可以恢复。
13. 事件设置页支持已有事件的标题编辑、置顶、归档和删除确认；归档与删除不会物理清除数据库记录。
14. 首页不再单独显示 FAB；底部时间轴导航在“大事件”首页提供同风格的加号入口，沿用真实新建表单、全天/精确时刻、标题、日期/时间校验和 Room 保存逻辑。
15. 删除后显示 Snackbar 撤销动作；撤销通过 Repository 恢复软删除事件，Snackbar 避开底部时间轴导航的加号入口。
16. 事件卡、搜索、设置、新建和时间轴导航节点提供明确的中文无障碍语义；事件卡的完整描述与点击动作覆盖同一卡片边界。
17. 首页 Hero 使用 `AdaptiveBackgroundPaletteAnalyzer` 和 `AdaptiveTransitionLayer`，根据背景图的环境色、明暗度、饱和度和复杂度生成连续过渡；现有卡片内容通过 `AdaptiveCardSurface` 作为半透明纸张容器显示。
18. 时间轴导航由首页与日子簿共用的长扁圆角胶囊、低对比度节点和“大事件 / 日子簿”两个入口组成；点击“大事件”回到主页，点击“日子簿”进入日历页。两页均在末端显示同材质圆形加号：首页创建默认日期事件，日子簿创建时预填当前选中日期。底部导航以覆盖层浮在内容之上，并通过底部安全滚动空间避免遮挡最后内容。
19. 模板浏览页以当前、上方预览、下方预览三张卡组成有界空间堆叠；纵向手势与“随机看看”共用连续位置、缩放、透明度和轻量 `rotationX` 过渡。所有分类行共享 Small/Wide 的连续槽位；相邻行先只显示主卡，左右尺寸预览会随其靠近中心渐显，避免纵向选中切换时跳变。顶部只保留左返回及右侧搜索/收藏的同规格暖白悬浮圆钮，不显示滑动提示文字；搜索在按钮下展开。
20. 首页支持长按卡片进入“编辑布局”：卡片可被拖动、相邻卡片以弹性位移动让，并将 `cardId / order / gridWidth / gridHeight` 本地保存。当前渲染兼容既有 Small 一槽与 Wide 跨两槽；未来模板可复用同一布局记录。

当前不属于已完成功能：回收站 UI、批量恢复、持久化搜索、Widget、通知、导入导出、真实错误/加载状态、真实设备回归、字体缩放和 TalkBack 全量验证；主页尚未提供真正的 2×2 高度跨度模板，布局的 `gridHeight` 已作为兼容接口保存。

## 当前阶段：分类与分组抽屉联动 + 阶段 7B 质量加固

## 日子簿页面雏形（2026-08-21）

- 底部时间轴的“日子簿”入口已接入独立 Compose 页面，不改变“大事件”首页与既有卡片模板。
- 页面首版聚焦月视图、今天/选中态、日期下方事件横杠和当日事件列表；空日期整块详情区可点击新增，已有事件时详情标题右侧提供主动新增按钮。系统事件使用暖色横杠，用户记录使用品牌蓝横杠，每日最多展示 3 条并以 `+` 折叠更多。
- 日子簿在 edge-to-edge 模式下由外层状态栏安全区避让顶部摄像头/刘海，月份切换不允许覆盖系统状态区域。
- `DaybookEvent` 明确区分 `source = SYSTEM / USER` 和 `eventType = FESTIVAL / SOLAR_TERM / ANNIVERSARY / PERSONAL / REMINDER`；用户事件保留 `showInMilestone`，默认不进入“大事件”筛选流，当前置顶事件可作为后续同步候选。
- `DaybookDataSource` 提供 `eventsForMonth` 与 `eventsForDate` 接口；当前 `MockDaybookDataSource` 将年度系统节日与已有 `TimeEvent` 映射为展示数据，真实 Room/节日数据源可直接替换。
- 从日历日期和空状态进入新增日期页时，统一携带所选 `LocalDate`；新增页已支持该日期预填，返回和保存都会回到进入它的来源页。当前日子簿仍使用 mock 事件，未新增 Room 表或迁移。
- 未验证边界：系统节假日/农历数据尚未接入，日子簿事件尚未独立持久化，真实设备视觉与 TalkBack 仍需复核。

阶段 7B 的响应式、字体缩放和无障碍语义约束继续有效；分类与分组抽屉新增能力已限定在现有事件/分组数据边界内：

- 事件事实字段由 Room `TimeEventEntity` 保存；状态、相对时间和星期仍由 `EventTimeCalculator` 派生。
- 数据库版本为 v2，必须保留并测试 v1→v2 migration。
- Repository 负责 DAO 编排和写入形状校验；软删除使用 `deletedAt`。
- DataStore 负责三态主题设置；未知值回退 `SYSTEM`。
- 首页从 Room Flow 读取既有样例和新增事件；模板画廊继续是内存预览；归档和软删除事件从活动流隐藏。
- 新建表单的 `ALL_DAY` 使用 `LocalDate`，`TIMED` 使用 `Instant + ZoneId`，并经 Repository 校验后保存。
- 新增日期页默认选择 `ALL_DAY`；返回按钮固定在状态栏安全区下方。日期与时间采用自适应等宽选择格，窄屏改为纵向排列并截断长文本，避免控件挤压换行。
- 删除 Snackbar 的撤销动作只恢复最近一次软删除事件，不改变事件事实字段。
- 首页视觉融合只修改 Hero 背景层和卡片外层容器，不删除或替换已有卡片模板，不改变 Small/Wide 网格规则。

自动验收已执行：14 个 JUnit 测试通过，`assembleDebug` 成功，`lintDebug` 为 0 error、15 个非阻断的版本/工具提示；6 个 Android instrumentation tests 在 Pixel 9 Pro API 35 模拟器通过，其中包含 Compose UI 语义/200% 字体测试。模拟器已完成 130%/200% 字体缩放和 2856×1280 宽屏尺寸覆盖下的关键语义检查，并完成真实新增、删除 Snackbar 展示和立即点击撤销恢复验证。真实 TalkBack 朗读、真实设备和撤销后的跨重启行为仍未验证。

阶段 7 明确不实现回收站、Widget、通知、网络或账号。

## 首发核心能力（v0.1）

- 新建、查看、编辑、置顶、归档、删除并撤销事件。
- 全天事件 / 精确时刻；自动显示“还有 / 今天 / 已经”。
- 本地持久化事件、顺序、分组、模板、颜色、主题和 Widget 配置。
- 首页筛选与搜索；空、加载、错误状态完整。
- 深色模式、响应式布局、字体缩放与 TalkBack 基本可用。
- 2×2 单事件 Widget，数据变更、删除、跨午夜、重启和时区变化后正确刷新。
- 单元测试、Compose UI 测试、Room 迁移测试与 Debug/Release 构建门禁。

## v0.2 再实现

- 多尺寸 Widget、提醒、每周/月/年重复。
- 本地 JSON 导入导出、回收站。
- “计入事件当天”等高级时间规则。
- 分享海报与更多模板。

## 暂不实现

- 登录、云同步、好友社交、公开广场。
- AI 自动生成事件。
- 农历、复杂节假日库。
- 日记/相册、待办、番茄钟。
- Widget 每秒刷新。

## 不可破坏的产品语义

- 日期在过去还是未来由系统判断，不要求用户先理解“正计时/倒计时”。
- Today 是独立状态，不能显示机械的 `0 天`。
- 全天事件不能用毫秒除以 `86,400,000` 计算。
- 精确时刻的绝对时刻不随设备时区变化，只改变本地展示。
- 规划中的 UI 不做假按钮；若暂时不能完整实现，明确显示限制或移除入口。
- 每增加一个功能，都不能让“创建一个事件并看到时间”这条核心路径变长。
